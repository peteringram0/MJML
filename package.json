{
  "name": "mjml-play",
  "version": "1.0.0",
  "description": "Trying out MJML for responsive emails",
  "main": "index.js",
  "scripts": {
    "watch": "mjml -w src/index.mjml --output build/email.html",
  	"build": "mjml -r src/index.mjml --output build/email.html",
    "show": "cd build && live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mjml": "^1.0.3"
  }
}
